# 封面生成提示词：柔和等距 3D 风

- 尺寸：1600×900（16:9）
- 生成状态：当前环境未提供 `imagegen` 工具，未生成位图。

```text
16:9 技术文章封面，1600×900。

一幅可爱、柔和的等距 3D 插画，主题是：将 EPUB 小说转换为可检索、可问答的 RAG 知识库。

画面从左到右展示清晰流程：一本电子书 → 被拆分成多个文本片段 → 转换为抽象的向量数据 → 存入向量数据库 → 检索出相关文本片段 → 交给聊天助手生成基于资料的回答。

风格：简洁的低多边形软 3D 产品插画，圆润造型，半透明连接路径，柔和材质与长阴影。
构图：宽幅等距视角，所有关键元素位于画面中央 80% 安全区域，流程方向清楚。
氛围：温暖、平静、有教育意义。
配色：暖米色背景、柔和紫色、薄荷绿、金黄色点缀。
限制：不要标题文字、不要可读文字、不要中文字符、不要 Logo、不要品牌标识、不要水印、不要虚构指标、不要代码、不要仪表盘界面。
```
