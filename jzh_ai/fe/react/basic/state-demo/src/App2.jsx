// import {
//   useState
// } from 'react'

// function App() {
//   const [count, setCount] = useState(0)
//   const addCount = () => {
//     setCount(count => count + 1)
//     setCount(count => count + 1)
//     setCount(count => count + 1)
//     console.log(count)
    
//   }
//   return (
//     <>
//       <p>当前计数：{count}</p>
//       <button onClick={addCount}>增加</button>
//       {/* 因为 React 组件的 return 必须返回 一个根节点 。这两个元素是并列的
//       如果直接返回它们，JSX 无法确定该返回哪个单独的元素，因此会报错，例如：Adjacent JSX elements must be wrapped in an enclosing tag */}
//     </>
//   )
// }

// export default App
